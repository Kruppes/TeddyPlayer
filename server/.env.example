# TeddyCloud (same network as teddycloud-custom-tonie-manager)
# Use "http://docker" if running in same docker network as TeddyCloud
# Use actual IP/hostname if running standalone
TEDDYCLOUD_URL=http://docker
TEDDYCLOUD_API_BASE=/api
TEDDYCLOUD_TIMEOUT=30

# Spotify (get credentials from https://developer.spotify.com/dashboard)
SPOTIFY_CLIENT_ID=your_client_id
SPOTIFY_CLIENT_SECRET=your_client_secret
SPOTIFY_REDIRECT_URI=http://localhost:8000/callback

# Default playback target (sonos, airplay, spotify)
DEFAULT_PLAYBACK_TARGET=sonos
